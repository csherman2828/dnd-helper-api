meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{URL}}/auth
  body: json
  auth: none
}

body:json {
  {
    "email":"csherman2828@gmail.com",
    "password": "TestPassword2!"
  }
}

vars:post-response {
  session: res.body.session
}

script:post-response {
  const cookies = res.getHeader('set-cookie');
  
  if(cookies) {
    bru.setVar("cookie", cookies.join('; '));
  }
}
